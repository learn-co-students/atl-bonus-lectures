require_relative 'spec_helper'

describe "Gilded Rose" do
  it "should probably be able to add numbers" do
    expect(2 + 2).to eq(4)
  end
  # it "should probably fail if stuff doesn't work" do
  #   expect(2 * 2).to eq(5)
  # end
end
